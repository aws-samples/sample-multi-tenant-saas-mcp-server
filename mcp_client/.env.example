# AWS Configuration for Bedrock (Optional - will use AWS credential chain if not specified)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Existing Cognito configuration for local development
# COGNITO_USER_POOL_ID=your_cognito_pool_id
# COGNITO_CLIENT_ID=your_cognito_client_id
# COGNITO_REGION=your_cognito_aws_region

# Node setting for local development
# NODE_ENV=development

# Optional: Server port for local development (default: 3001)
# PORT=3001
